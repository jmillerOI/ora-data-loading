<div class="container">
  <div class="row">
    <div class="col-md-8">
      {{#each model as |photo|}}
        {{photo-locked-component photo=photo}}
      {{/each}}
    </div>
    <div class="col-md-4"></div>
  </div>
</div>